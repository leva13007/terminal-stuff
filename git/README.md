---
status: active
category: youtube
stack: git, shell
---

# git

Конспект зручних git-аліасів — рівня `.gitconfig` і рівня шела (zsh). Матеріал для відео про термінал/git workflow. Поки що чисто нотатки — нічого з цього не застосовано в реальному `~/.gitconfig` чи `~/.zshrc`.

## where I left off

Записав перелік аліасів (базові + просунуті) у `notes/01-aliases.md` з поясненням різниці git alias vs shell alias.

## next step

Обрати з конспекту фінальний набір і, за бажанням, застосувати вручну в `~/.gitconfig` / `~/.zshrc`.

## resources

- https://git-scm.com/book/en/v2/Git-Basics-Git-Aliases
